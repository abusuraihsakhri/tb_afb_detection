version: '3.8'

services:
  tb-afb-api:
    build: .
    restart: always
    ports:
      - "8001:8001"
#    deploy:
#      resources:
#        reservations:
#          devices:
#            - driver: nvidia
#              count: 1
#              capabilities: [gpu]
    volumes:
      - ./01_DATA:/app/01_DATA
      - ./03_MODELS:/app/03_MODELS
      - ./06_LOGS:/app/06_LOGS
    environment:
      - PYTHONUNBUFFERED=1
      - CHOKIDAR_USEPOLLING=true
